---
phase: 06-cleanup-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/_Project/Scripts/Snakes/SnakeAI.cs
autonomous: false

must_haves:
  truths:
    - "SnakeAI.cs compiles with zero errors and zero warnings"
    - "Snakes patrol, chase, and face the player exactly as in v1.8.4"
    - "Debug output is limited to LogWarning/LogError only — no verbose Debug.Log spam in play mode"
    - "Code comments accurately describe NavMesh-based implementation (no references to MoveTowardsSafe or Phase 1 static snakes)"
    - "Version header reads v1.8.5"
  artifacts:
    - path: "Assets/_Project/Scripts/Snakes/SnakeAI.cs"
      provides: "Cleaned-up SnakeAI v1.8.5"
      contains: "v1.8.5"
  key_links:
    - from: "UpdatePatrol()"
      to: "_isPatrolling bool"
      via: "still live — tracks waypoint start to prevent per-frame re-generation"
      pattern: "_isPatrolling"
    - from: "FollowPlayer() / HandleIdlePlayerInteraction()"
      to: "LookAtPlayer()"
      via: "still live — handles Y-axis facing toward player in all non-patrol states"
      pattern: "LookAtPlayer"
---

<objective>
Remove verbose Debug.Log calls and update stale code comments in SnakeAI.cs.
Produce v1.8.5: submission-clean, no runtime log spam, accurate documentation.

Purpose: The file still has 18+ active Debug.Log calls from development sessions.
These pollute the console in play mode and are not appropriate for submission.
Comments still reference "Phase 1 static snakes" and "No NavMesh yet" which are
completely outdated after Phase 5.

Output: SnakeAI.cs v1.8.5 — same behavior as v1.8.4, cleaner code.

IMPORTANT DECISIONS (do not second-guess these):
- LookAtPlayer() STAYS — it handles snake facing during idle interactions (bite/breath/projectile
  ranges). NavMeshAgent only drives position. Rotation is manual via LookAtPlayer() and the
  velocity-based patrol slerp. Deleting it breaks player-facing behavior.
- _isPatrolling bool STAYS — it is live state in UpdatePatrol(): guards the waypoint generation
  call (line 655-659) to prevent re-generation every single frame. Not dead code.
- The comment on Awake() line 439 "// Manual rotation via LookAtPlayer()" is ACCURATE — keep it.
</objective>

<execution_context>
@C:/Users/Teilnehmer/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Teilnehmer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Assets/_Project/Scripts/Snakes/SnakeAI.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove verbose Debug.Log calls and update stale comments</name>
  <files>Assets/_Project/Scripts/Snakes/SnakeAI.cs</files>
  <action>
Make the following changes to SnakeAI.cs. Read the file before editing.

=== 1. VERSION HEADER (line 9) ===
Change:
  * Version: 1.8.3 - NavMesh Full Migration (Phase 5 Plan 03)
To:
  * Version: 1.8.5 - Cleanup & Polish (Phase 6)

=== 2. ADD VERSION HISTORY ENTRY (after the v1.8.4 entry, before the closing ===) ===
Add:
  * - v1.8.5: Cleanup & Polish (Phase 6) — Submission-ready:
  *         REMOVED: All verbose Debug.Log() calls (runtime noise for submission)
  *         KEPT: Debug.LogWarning() (legitimate edge case reporting)
  *         UPDATED: Code comments to reflect NavMesh implementation (Phase 5 complete)
  *         NOTES: LookAtPlayer() RETAINED — handles player-facing in idle state ranges
  *         NOTES: _isPatrolling bool RETAINED — live state guard in UpdatePatrol()
  *         Result: Zero console spam in play mode, accurate documentation (2026-02-17)

=== 3. NOTES SECTION UPDATE (lines 37-42 in the header comment) ===
Change the NOTES block from:
  * NOTES:
  * - Phase 1 implementation — minimal viable snake
  * - No NavMesh patrol yet (Phase 2)
  * - No animations yet (waiting for Toon Snakes Pack import)
  * - Collider damage on contact when aggressive
To:
  * NOTES:
  * - Phase 5 implementation — full NavMesh movement
  * - NavMeshAgent drives position (updatePosition=false, manual LateUpdate sync)
  * - LookAtPlayer() handles rotation — NavMeshAgent updateRotation=false
  * - Toon Snakes Pack animations: Slither Forward/Left/Right, Bite, Breath, Die
  * - Collider damage on contact when aggressive

=== 4. REMOVE Debug.Log CALLS (keep Debug.LogWarning, remove Debug.Log) ===

Remove or comment out ONLY these Debug.Log lines (NOT LogWarning):

Line 428 — in Awake(), MoveAwayTarget detach log:
  Debug.Log($"SnakeAI ({_snakeName}): Detached MoveAwayTarget at START (World: {worldPos})");
  Action: DELETE this line (the detach logic above it stays)

Line 790 — in UpdateState(), MovedAway arrival log:
  Debug.Log($"SnakeAI ({_snakeName}): Reached MoveAwayTarget via NavMesh, transitioning to root state");
  Action: DELETE this line

Line 985 — in StartAttackingEnemy(), attack target log:
  Debug.Log($"SnakeAI ({_snakeName}): [ATTACK CREATURE] Target: '{targetCreature.name}' at distance {Vector3.Distance(transform.position, targetCreature.transform.position):F2}");
  Action: DELETE this line

Line 995 — in StartAttackingEnemy(), snake-vs-snake log:
  Debug.Log($"SnakeAI ({_snakeName}): [ATTACK CREATURE] Target is Snake, both will neutralize in 1.5s");
  Action: DELETE this line

Line 1001 — in StartAttackingEnemy(), non-snake log:
  Debug.Log($"SnakeAI ({_snakeName}): [ATTACK CREATURE] Target is NOT Snake, destroying in 1.5s");
  Action: DELETE this line

Line 1054 — in NeutralizeAfterAttack():
  Debug.Log($"SnakeAI ({_snakeName}): Neutralized after attack (Dead state)");
  Action: DELETE this line

Line 1070 — in TransitionFromMoveAwayToRootState():
  Debug.Log($"SnakeAI ({_snakeName}): MoveAway complete → Idle (Player visible: {_canSeePlayer}, Distance: {_playerDistance:F1})");
  Action: DELETE this line

Line 1119 — in SetState(), Daze end log:
  Debug.Log($"SnakeAI ({_snakeName}): [DAZE END] Leaving Dazed state, IsDazed=false, attack cooldown reset");
  Action: DELETE this line

Line 1127 — in SetState() Idle case:
  Debug.Log($"SnakeAI ({_snakeName}): [STATE] {previousState} → Idle");
  Action: DELETE this line

Line 1134 — in SetState() Aggressive case:
  Debug.Log($"SnakeAI ({_snakeName}): [STATE] {previousState} → Aggressive (timer: {_stateTimer:F1}s, Red glow)");
  Action: DELETE this line

Line 1143 — in SetState() MovedAway case:
  Debug.Log($"SnakeAI ({_snakeName}): [SPELL] Tune 1 (Move) → MovedAway (White glow, collision OFF, delay: {_spellAnimationDelay}s)");
  Action: DELETE this line

Line 1155 — in SetState() Dazed case:
  Debug.Log($"SnakeAI ({_snakeName}): [SPELL] Tune 2 (Daze) → Dazed (Blue glow, IsDazed=true, collision OFF, timer: {_stateTimer:F1}s)");
  Action: DELETE this line

Line 1163 — in SetState() AttackingEnemy case:
  Debug.Log($"SnakeAI ({_snakeName}): [SPELL] Tune 3 (Attack) → AttackingEnemy (Yellow glow, collision OFF, delay: {_spellAnimationDelay}s)");
  Action: DELETE this line

Line 1170 — in SetState() Frozen case:
  Debug.Log($"SnakeAI ({_snakeName}): [SPELL] Tune 4 (Freeze) → Frozen (Cyan glow, collision ON, timer: {_stateTimer:F1}s)");
  Action: DELETE this line

Line 1182 — in SetState() Dead case:
  Debug.Log($"SnakeAI ({_snakeName}): [STATE] {previousState} → Dead (Gray, collision OFF, Die trigger, IsDazed=true)");
  Action: DELETE this line

Line 1404 — in TriggerAttack():
  Debug.Log($"SnakeAI ({_snakeName}): [ATTACK] {attackType} triggered! (Damage: {damage}, Distance: {_playerDistance:F2}, Delay: {damageDelay}s)");
  Action: DELETE this line

DO NOT TOUCH:
- Debug.LogWarning at line 456 (no player found — legitimate warning)
- Debug.LogWarning at line 728 (no valid patrol waypoint — legitimate warning)
- Debug.LogWarning at line 795 (MovedAway with no target — legitimate warning)
- Debug.LogWarning at line 1008 (no targetable creature found — legitimate warning)
- Debug.LogWarning at line 1526 (renderer not found — legitimate warning)
- All commented-out Debug.Log lines (// Debug.Log...) — already silenced, leave them

=== 5. DO NOT CHANGE ===
- LookAtPlayer() method and all its call sites
- _isPatrolling field and all its usages in UpdatePatrol()
- All game logic, state machine, NavMesh code
- All [SerializeField] fields and Inspector headers
- All XML documentation comments (///)
- All #region/#endregion structure
  </action>
  <verify>
Open Unity Editor. Check the Console window in Play mode — it should show no Debug.Log
output during normal snake patrol and state transitions. Only Debug.LogWarning may appear
(e.g., "No valid patrol waypoint found" only when that fallback triggers).

If Unity is not open, verify by searching the file:
  grep -n "Debug.Log(" Assets/_Project/Scripts/Snakes/SnakeAI.cs
The result should show ONLY lines containing "Debug.LogWarning" — no bare "Debug.Log(" calls.

Also verify version header:
  grep "Version:" Assets/_Project/Scripts/Snakes/SnakeAI.cs
Should return: * Version: 1.8.5 - Cleanup & Polish (Phase 6)
  </verify>
  <done>
- Version header reads "v1.8.5 - Cleanup & Polish (Phase 6)"
- v1.8.5 entry added to VERSION HISTORY
- NOTES section updated to reflect Phase 5 NavMesh implementation
- All 16 verbose Debug.Log() calls removed
- All 5 Debug.LogWarning() calls preserved
- LookAtPlayer() untouched, _isPatrolling untouched
- File compiles without errors or warnings
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  SnakeAI.cs v1.8.5 — same NavMesh behavior as v1.8.4 with all verbose Debug.Log
  calls removed and stale comments updated.
  </what-built>
  <how-to-verify>
  1. Open Unity Editor, open GameLevel scene
  2. Clear the Console (right-click → Clear)
  3. Press Play
  4. Observe Console for 30 seconds while snakes patrol:
     - EXPECTED: No Debug.Log output during normal patrol/idle
     - EXPECTED: No state-change messages (Idle → Aggressive, etc.)
     - ALLOWED: Debug.LogWarning if a snake cannot find a patrol waypoint
  5. Walk near a snake until it detects you:
     - EXPECTED: No attack log messages in Console
     - EXPECTED: Snake still faces you and attacks normally
  6. Cast a Tune (press 1, hold, release in zone):
     - EXPECTED: No spell log messages in Console
     - EXPECTED: Snake still responds to tune correctly
  7. Check SnakeAI.cs header in any code editor:
     - Version should read "1.8.5"
  </how-to-verify>
  <resume-signal>Type "approved" if Console is clean and snake behavior unchanged. Describe any issues if not.</resume-signal>
</task>

</tasks>

<verification>
After Task 1 completes, run a grep check to confirm no bare Debug.Log remains:
The file should contain "Debug.LogWarning" occurrences only (no "Debug.Log(" without "Warning").

After checkpoint approval, commit:
  git add Assets/_Project/Scripts/Snakes/SnakeAI.cs
  git commit -m "refactor(06-01): SnakeAI v1.8.5 - remove Debug.Log spam, update comments"
</verification>

<success_criteria>
- Zero Debug.Log() calls remaining (only Debug.LogWarning preserved)
- Version header: v1.8.5
- Unity Console clean during normal play mode (no log spam)
- All snake behaviors identical to v1.8.4 (LookAtPlayer + _isPatrolling untouched)
- No compiler errors or warnings
- Commit created with message "refactor(06-01): SnakeAI v1.8.5 - remove Debug.Log spam, update comments"
</success_criteria>

<output>
After completion, create `.planning/phases/06-cleanup-polish/06-01-SUMMARY.md` with:
- What was changed (list of removed Debug.Log calls, comment updates)
- What was intentionally kept (LookAtPlayer, _isPatrolling) and why
- Confirmation that behavior is identical to v1.8.4
- Git commit hash
</output>
