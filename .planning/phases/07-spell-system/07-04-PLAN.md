---
phase: 07-spell-system
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - Assets/_Project/Scripts/TuneSystem/TuneController.cs
  - Assets/_Project/Scripts/Player/HealthSystem.cs
  - Assets/_Project/Scripts/Snakes/SnakeAI.cs
  - Assets/_Project/Scripts/UI/SpellHUDController.cs
autonomous: false

must_haves:
  truths:
    - "Move and Daze require a snake in range to cast — if no snake in range, cast is blocked silently"
    - "Shield is castable anywhere — no range check needed"
    - "Shield cannot be recast while already active — Tune 3 key is non-responsive while shield is up"
    - "HP heals ONLY when Move or Daze actually charms a snake (not on Shield, not on empty casts)"
    - "All spells have a cooldown timer that prevents recasting until expired"
    - "Advanced mode has limited charges per spell that do not regenerate"
    - "HUD shows range indicator (subtle) when castable snake is in range"
    - "HUD shows cooldown state on each slot"
  artifacts:
    - path: "Assets/_Project/Scripts/TuneSystem/TuneController.cs"
      provides: "Range check, cooldown, charges, Shield activation call"
      contains: "HasSnakeInRange"
    - path: "Assets/_Project/Scripts/Player/HealthSystem.cs"
      provides: "Heal-on-charm subscription (OnSnakeCharmed instead of OnTuneSuccess)"
      contains: "OnSnakeCharmed"
    - path: "Assets/_Project/Scripts/Snakes/SnakeAI.cs"
      provides: "Fires GameEvents.SnakeCharmed() when Move/Daze actually takes effect"
      contains: "GameEvents.SnakeCharmed"
    - path: "Assets/_Project/Scripts/UI/SpellHUDController.cs"
      provides: "Range indicator + cooldown overlay on tune slots"
      contains: "OnTuneCooldownStarted"
  key_links:
    - from: "SnakeAI.cs"
      to: "GameEvents.cs"
      via: "fires SnakeCharmed(tuneNumber) in ApplyTuneEffect for Move and Daze"
      pattern: "GameEvents\\.SnakeCharmed"
    - from: "HealthSystem.cs"
      to: "GameEvents.cs"
      via: "subscribes to OnSnakeCharmed instead of OnTuneSuccess"
      pattern: "OnSnakeCharmed"
    - from: "TuneController.cs"
      to: "ShieldComponent.cs"
      via: "checks IsShieldActive before allowing Tune 3 cast, calls ActivateShield on Tune 3 success"
      pattern: "ActivateShield|IsShieldActive"
    - from: "TuneController.cs"
      to: "Physics.OverlapSphere"
      via: "range check before casting Move/Daze"
      pattern: "OverlapSphere"
    - from: "SpellHUDController.cs"
      to: "GameEvents.cs"
      via: "subscribes to OnTuneCooldownStarted, OnTuneCooldownExpired, OnSnakeInRangeChanged"
      pattern: "OnTuneCooldown|OnSnakeInRange"
---

<objective>
Wire spell casting rules into TuneController (range check, cooldown, charges, Shield activation), fix heal-on-charm in HealthSystem/SnakeAI, and add HUD indicators for range and cooldown.

Purpose: Complete the spell system with all gameplay rules — range gating, cooldowns, Advanced mode charges, and heal-on-charm-only.
Output: Fully functional spell casting system with all rules enforced and HUD feedback
</objective>

<execution_context>
@C:/Users/Teilnehmer/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Teilnehmer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-spell-system/07-CONTEXT.md
@.planning/phases/07-spell-system/07-RESEARCH.md
@.planning/phases/07-spell-system/07-02-SUMMARY.md
@.planning/phases/07-spell-system/07-03-SUMMARY.md
@Assets/_Project/Scripts/TuneSystem/TuneController.cs
@Assets/_Project/Scripts/Player/HealthSystem.cs
@Assets/_Project/Scripts/Snakes/SnakeAI.cs
@Assets/_Project/Scripts/UI/SpellHUDController.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add range check, cooldown, charges, and Shield wiring to TuneController</name>
  <files>Assets/_Project/Scripts/TuneSystem/TuneController.cs</files>
  <action>
Add spell casting rules to TuneController v3.0 (from plan 07-02). These are additions to the existing refactored code.

**New SerializeFields:**
- `float _spellCastRange = 8f` — range for Move/Daze casting (snake must be within this)
- `float[] _cooldownDurations = { 3f, 5f, 8f }` — cooldown per tune (Move, Daze, Shield)
- `int[] _spellCharges = { 5, 5, 3 }` — charges for Advanced mode (placeholder values per user decision — Phase 13 balancing)
- `LayerMask _snakeLayerMask` — for OverlapSphere (optional, can use default)

**New private fields:**
- `float[] _cooldownTimers = new float[3]` — remaining cooldown time
- `int[] _remainingCharges = new int[3]` — remaining charges (Advanced mode)
- `ShieldComponent _shieldComponent` — cached reference
- `bool _lastSnakeInRange = false` — for range change event debounce

**In Awake():**
- Cache: `_shieldComponent = GetComponent<ShieldComponent>();` (or GetComponentInParent if TuneController is on a child)
- Initialize charges: `if (!_isSimpleMode) _remainingCharges = (int[])_spellCharges.Clone();`

**In Update():**
After existing slider update, add:
1. **Cooldown tick**: `for (int i = 0; i < 3; i++) { if (_cooldownTimers[i] > 0f) { _cooldownTimers[i] -= Time.deltaTime; if (_cooldownTimers[i] <= 0f) { _cooldownTimers[i] = 0f; GameEvents.TuneCooldownExpired(i + 1); } } }`
2. **Range indicator update** (only when not casting): Check `HasSnakeInRange(_spellCastRange)` and compare to `_lastSnakeInRange`. If changed, fire `GameEvents.SnakeInRangeChanged(newValue)` and update `_lastSnakeInRange`.

**Modify OnTuneKeyPressed(int tuneNumber):**
After existing unlock guard (`if (!_tuneUnlocked[idx]) return;`), add these guards in order:

1. **Cooldown guard**: `if (_cooldownTimers[idx] > 0f) return;` — silently blocked
2. **Charge guard** (Advanced mode only): `if (!_isSimpleMode && _remainingCharges[idx] <= 0) return;` — no charges left
3. **Range guard for Move/Daze** (tunes 1 and 2): `if (tuneNumber <= 2 && !HasSnakeInRange(_spellCastRange)) return;` — no snake in range, block cast
4. **Shield active guard** (tune 3 only): `if (tuneNumber == 3 && _shieldComponent != null && _shieldComponent.IsShieldActive) return;` — cannot recast while active

**Modify EndTune() — Success case:**
After existing `GameEvents.TuneSuccess()` and `GameEvents.TuneSuccessWithId(tuneNumber)` calls:

1. **Start cooldown**: `_cooldownTimers[tuneNumber - 1] = _cooldownDurations[tuneNumber - 1]; GameEvents.TuneCooldownStarted(tuneNumber, _cooldownDurations[tuneNumber - 1]);`
2. **Consume charge** (Advanced mode): `if (!_isSimpleMode) _remainingCharges[tuneNumber - 1]--;`
3. **Shield activation** (tune 3 only): `if (tuneNumber == 3 && _shieldComponent != null) { _shieldComponent.ActivateShield(); }` — directly activate shield on Tune 3 success
4. **For Tune 3 (Shield): Do NOT fire `GameEvents.TuneSuccessWithId(3)`** — Shield has no snake effect. Replace the unconditional TuneSuccessWithId with a conditional: `if (tuneNumber <= 2) GameEvents.TuneSuccessWithId(tuneNumber);` — only Move/Daze fire the snake-targeting event.

**Add HasSnakeInRange method:**
```csharp
private bool HasSnakeInRange(float range)
{
    Collider[] hits = Physics.OverlapSphere(transform.position, range);
    foreach (var hit in hits)
    {
        if (hit.GetComponent<SnakeEnchanter.Snakes.SnakeAI>() != null)
            return true;
    }
    return false;
}
```

**Update version header** to v3.1 — "Spell casting rules: range, cooldown, charges, Shield wiring"

Add `using SnakeEnchanter.Player;` for ShieldComponent access if not already present.
  </action>
  <verify>
1. TuneController compiles
2. Move/Daze blocked when no snake in range (HasSnakeInRange returns false)
3. Shield castable anywhere (no range check for tune 3)
4. Shield cannot recast while active
5. Cooldown timers tick down in Update, prevent recasting
6. Advanced mode charges consumed on success, block when depleted
7. Tune 3 success activates shield and does NOT fire TuneSuccessWithId
8. Tune 1/2 success fires TuneSuccessWithId (snakes react) AND starts cooldown
  </verify>
  <done>TuneController v3.1 enforces all spell casting rules: range gating for Move/Daze, cooldown on all spells, charges for Advanced mode, Shield activation + no-recast-while-active, and TuneSuccessWithId only fires for snake-targeting tunes.</done>
</task>

<task type="auto">
  <name>Task 2a: Fix heal-on-charm in HealthSystem</name>
  <files>Assets/_Project/Scripts/Player/HealthSystem.cs</files>
  <action>
Change HealthSystem to heal via OnSnakeCharmed instead of OnTuneSuccess. HP heals ONLY when a snake is actually charmed by Move or Daze. Not on Shield. Not on empty casts.

**HealthSystem.cs changes:**

1. **Remove** `OnTuneSuccess` subscription:
   - In `OnEnable()`: remove `GameEvents.OnTuneSuccess += OnTuneSuccessHealing;`
   - In `OnDisable()`: remove `GameEvents.OnTuneSuccess -= OnTuneSuccessHealing;`

2. **Add** `OnSnakeCharmed` subscription:
   - In `OnEnable()`: add `GameEvents.OnSnakeCharmed += OnSnakeCharmedHealing;`
   - In `OnDisable()`: add `GameEvents.OnSnakeCharmed -= OnSnakeCharmedHealing;`

3. **Add new handler:**
```csharp
private void OnSnakeCharmedHealing(int tuneNumber)
{
    // Only heal when a snake is actually charmed (Move or Daze)
    // Shield casts (tune 3) do NOT heal
    if (tuneNumber >= 1 && tuneNumber <= 2)
    {
        Heal(_healPerTuneSuccess);
    }
}
```

4. **Remove** the old `OnTuneSuccessHealing()` method entirely — it is now replaced by `OnSnakeCharmedHealing`.

5. **Update version header** to v1.5 — "Heal-on-charm: only heals when snake is actually charmed"
  </action>
  <verify>
1. HealthSystem no longer subscribes to OnTuneSuccess
2. HealthSystem subscribes to OnSnakeCharmed and heals only for tune 1 and 2
3. Old OnTuneSuccessHealing method is removed
4. Casting Shield (tune 3) does NOT heal the player
5. Casting Move/Daze with no snake in range (blocked by TuneController) does NOT heal
6. Version header updated to v1.5
  </verify>
  <done>HealthSystem heals via OnSnakeCharmed event instead of OnTuneSuccess. Shield casts and empty casts produce no healing.</done>
</task>

<task type="auto">
  <name>Task 2b: SnakeAI — fire SnakeCharmed event + remove all Attack/Freeze dead code</name>
  <files>Assets/_Project/Scripts/Snakes/SnakeAI.cs</files>
  <action>
This task has TWO parts: (A) wire up SnakeCharmed event for heal-on-charm, and (B) remove all dead code from the removed Attack/Freeze features.

**PART A — Fire SnakeCharmed event:**

Update `ApplyTuneEffect(SnakeEffect effect)` to fire `GameEvents.SnakeCharmed()` when Move or Daze takes effect:

```csharp
public void ApplyTuneEffect(SnakeEffect effect)
{
    switch (effect)
    {
        case SnakeEffect.Move:
            SetState(SnakeState.MovedAway);
            GameEvents.SnakeCharmed(1); // Heal triggers here
            break;

        case SnakeEffect.Daze:
            SetState(SnakeState.Dazed);
            GameEvents.SnakeCharmed(2); // Heal triggers here
            break;

        // Shield has no snake effect — SnakeAI never processes it
    }
}
```

Update `OnTuneSuccessWithId()` tune-to-effect mapping:
- Remove: `case 3 => SnakeEffect.Attack` (Attack enum value removed in 07-01)
- Remove: Tune 4 Freeze special case entirely (Freeze is gone)
- Update to: `1 => SnakeEffect.Move, 2 => SnakeEffect.Daze, _ => SnakeEffect.Move`
- Note: TuneController no longer fires TuneSuccessWithId for tune 3 (handled in Task 1)

**PART B — Remove Attack/Freeze dead code:**

After removing SnakeEffect.Attack and SnakeEffect.Freeze from the enum (07-01), SnakeAI has ~50+ lines of dead code that must be cleaned up:

1. **SnakeState enum**: Remove `AttackingEnemy` and `Frozen` values from the enum.

2. **SerializeField removal**: Remove `_freezeDuration` field (line ~264) and `_frozenColor` field (line ~328).

3. **SetState() cleanup**: Remove the `case SnakeState.AttackingEnemy:` block (lines ~1152-1157) and `case SnakeState.Frozen:` block (lines ~1159-1163) from the switch statement. Also remove `case SnakeState.AttackingEnemy:` from the NavMeshAgent switch in SetState() (lines ~1094-1098 — merge the logic: Dazed/Dead cases remain, AttackingEnemy removed).

4. **UpdateState() cleanup**: Remove `case SnakeState.Frozen:` block (lines ~815-821) and `case SnakeState.AttackingEnemy:` block (lines ~823-826).

5. **Remove methods**:
   - `StartAttackingEnemy()` (lines ~969-1013) — entire method
   - `FindNearestCreature()` (lines ~1020-1046) — entire method
   - `NeutralizeAfterAttack()` (lines ~1051-1056) — entire method
   - `ApplyFreeze()` (lines ~1552-1555) — entire method

6. **AUTHORSHIP header**: Remove references to "Attack system (v1.1)" attack creature behaviors, and "Charmed/Dazed/Attacking/Frozen" (update to "Charmed/Dazed"). Update state machine description comment.

7. **Version header comments**: Clean up v1.5.0 entry (references to Attack/Freeze behaviors).

**Update SnakeAI version header** to v1.9 — "Phase 7: fires SnakeCharmed event, removes Attack/Freeze dead code (SnakeEffect.Attack + SnakeEffect.Freeze removed from enum)"

IMPORTANT: Preserve ALL other existing behavior — patrol, proximity, attack system (Bite/Breath/Projectile from snakes to player), visual feedback, movement animations. Only remove code specific to the removed SnakeEffect.Attack (Tune 3 old behavior) and SnakeEffect.Freeze (Tune 4 old behavior).
  </action>
  <verify>
1. SnakeAI.ApplyTuneEffect fires GameEvents.SnakeCharmed(1) for Move, SnakeCharmed(2) for Daze
2. SnakeAI.OnTuneSuccessWithId no longer maps tune 3 to Attack or tune 4 to Freeze
3. SnakeState enum no longer contains AttackingEnemy or Frozen
4. SetState() has no case for AttackingEnemy or Frozen
5. UpdateState() has no case for AttackingEnemy or Frozen
6. StartAttackingEnemy(), FindNearestCreature(), NeutralizeAfterAttack(), ApplyFreeze() methods are removed
7. _freezeDuration and _frozenColor fields are removed
8. NavMeshAgent switch in SetState() no longer references AttackingEnemy or Frozen
9. All patrol, proximity, snake-to-player attack, visual feedback code is preserved
10. No compilation errors — no references to removed enum values or methods remain
  </verify>
  <done>SnakeAI fires SnakeCharmed event on Move/Daze, and all Attack/Freeze dead code (states, methods, fields) is removed. Only Move/Daze/Shield tune effects remain.</done>
</task>

<task type="auto">
  <name>Task 3: Add cooldown + range indicator to SpellHUDController</name>
  <files>Assets/_Project/Scripts/UI/SpellHUDController.cs</files>
  <action>
Add cooldown and range indicator visual feedback to SpellHUDController.cs (created in 07-02). These are event-driven HUD updates that subscribe to GameEvents from TuneController (Task 1).

**New event subscriptions in OnEnable() / OnDisable():**
- `GameEvents.OnTuneCooldownStarted += OnCooldownStarted;`
- `GameEvents.OnTuneCooldownExpired += OnCooldownExpired;`
- `GameEvents.OnSnakeInRangeChanged += OnSnakeInRangeChanged;`

**Cooldown display:**

Add `Image[] _cooldownOverlays = new Image[3]` — one per slot (found as child "CooldownOverlay" in slot prefab).

`OnCooldownStarted(int tuneNumber, float duration)`:
1. `int idx = tuneNumber - 1;` + bounds check
2. If `_slots[idx] == null` return (slot not yet created)
3. Find "CooldownOverlay" Image child on `_slots[idx]` — set active, set fillAmount to 1
4. Start coroutine `CooldownTickCoroutine(idx, duration)` — lerps fillAmount from 1 to 0 over `duration` seconds using `Time.deltaTime`

`OnCooldownExpired(int tuneNumber)`:
1. `int idx = tuneNumber - 1;` + bounds check
2. If `_cooldownOverlays[idx] != null`: set fillAmount to 0, hide overlay

When creating slots in OnTuneUnlocked, also find and cache "CooldownOverlay" into `_cooldownOverlays[idx]`, hidden by default.

**Range indicator:**

Add `Image _rangeIndicatorBorder` or use existing slot border — subtle highlight when snake is in range.

`OnSnakeInRangeChanged(bool inRange)`:
- Loop through all active (non-null) `_slots` — for slots 0 and 1 (Move and Daze only), toggle a subtle border highlight or glow color:
  - `inRange = true`: set slot border/outline color to a bright highlight (e.g., white or tune color at higher alpha)
  - `inRange = false`: set back to default subtle color
- Slot 2 (Shield) is unaffected by range (Shield is self-targeted)

**Update slot prefab structure comment** to include:
```
SlotPrefab (RectTransform, CanvasGroup)
  ├── Background (Image — colored bg)
  ├── KeyIcon (Image — physical key shape sprite)
  │   └── KeyLabel (TextMeshProUGUI — "1", "2", "3")
  ├── SpellName (TextMeshProUGUI — "Move", "Daze", "Shield")
  └── CooldownOverlay (Image — fillAmount radial overlay, initially hidden)
```

**Update version header** to v1.1 — "Cooldown display + range indicator"
  </action>
  <verify>
1. SpellHUDController subscribes to OnTuneCooldownStarted, OnTuneCooldownExpired, OnSnakeInRangeChanged
2. Cooldown overlay fills radially and drains over duration
3. Cooldown overlay hides when expired
4. Range indicator highlights Move/Daze slots when snake is in range
5. Shield slot (index 2) is NOT affected by range indicator
6. No compilation errors
  </verify>
  <done>SpellHUDController shows cooldown progress overlay on each slot and highlights Move/Daze slots when a snake is in casting range.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify full spell system in Unity Editor</name>
  <files>Assets/_Project/Scenes/GameLevel.unity</files>
  <action>
Human verification of the complete spell system across all 4 plans. What was built:
- Scroll pickup with proximity glow and Zelda-style pause panel (07-01)
- TuneController refactored to 3-tune array with unlock gate (07-02)
- Dynamic HUD that starts empty and grows with scroll collection (07-02)
- Shield blocks one attack for 8s with screen edge glow (07-03)
- Range check blocks Move/Daze when no snake nearby (07-04)
- Cooldown prevents rapid recasting (07-04)
- Advanced mode charges limit total casts (07-04)
- Heal only fires when snake is actually charmed (07-04)

Steps to verify:
1. Enter Play Mode in GameLevel scene
2. Verify HUD is empty — no tune slots visible at start
3. Press 1, 2, 3 — should do nothing (tunes locked)
4. Walk to first scroll — verify proximity glow brightens as you approach
5. Walk over scroll — verify game pauses, panel shows scroll name + description + key number
6. Press any key — panel dismisses, game resumes, HUD shows 1 new slot with fade-in
7. Press the unlocked tune key near a snake — verify slider works, success charms the snake, HP heals
8. Press the unlocked tune key with NO snake nearby — verify cast is blocked (Move/Daze)
9. Collect all 3 scrolls — HUD shows 3 slots
10. Cast Shield (key 3) — verify screen edge glow appears, lasts ~8s
11. Cast Shield then get attacked — verify shield absorbs attack (no HP loss), flash effect, shield breaks
12. Try recasting Shield while active — verify key 3 does nothing
13. Cast any spell twice quickly — verify cooldown prevents second cast
14. In Advanced mode: Verify charges deplete after each cast

Type "approved" or describe any issues found to continue.
  </action>
  <verify>User confirms all 14 verification steps pass in Unity Editor play mode</verify>
  <done>Full spell system verified by human: scroll collection, unlock progression, 3 tunes, shield mechanic, range check, cooldown, charges, heal-on-charm all working correctly</done>
</task>

</tasks>

<verification>
1. TuneController enforces range (Move/Daze), cooldown (all), charges (Advanced), Shield no-recast
2. HealthSystem heals via OnSnakeCharmed (not OnTuneSuccess)
3. SnakeAI fires SnakeCharmed when Move/Daze actually takes effect
4. Shield blocks one attack then breaks, or expires after 8s
5. HUD shows range indicator and cooldown state
6. TuneSuccessWithId only fires for tunes 1 and 2 (not Shield)
7. All casting rules work together without conflicts
</verification>

<success_criteria>
- Full spell system functional: scroll collection, unlock progression, 3 tunes with distinct behaviors
- All casting rules enforced: range, cooldown, charges, heal-on-charm
- Shield mechanic works: activate, absorb/expire, visual feedback
- Human verification confirms gameplay feels correct
</success_criteria>

<output>
After completion, create `.planning/phases/07-spell-system/07-04-SUMMARY.md`
</output>
