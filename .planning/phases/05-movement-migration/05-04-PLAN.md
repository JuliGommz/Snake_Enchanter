---
phase: 05-movement-migration
plan: "04"
type: execute
wave: 4
depends_on: ["05-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Snake patrol animation does NOT restart from frame 0 when approaching a wall"
    - "Snake navigates around cave obstacles (not through them)"
    - "Tune 1 (Move) causes snake to navigate to MoveAwayTarget via NavMesh"
    - "Tune 2 (Daze) collapses snake and stops all movement immediately"
    - "Failed tune causes snake to chase player (aggressive state)"
    - "Console shows no errors during gameplay"
  artifacts:
    - path: "Assets/_Project/Scripts/Snakes/SnakeAI.cs"
      provides: "v1.8.3 full NavMesh migration"
      contains: "_agent.velocity.magnitude"
  key_links:
    - from: "Patrol animation"
      to: "agent.velocity"
      via: "velocity=0 when blocked, animation holds frame (bug fixed)"
      pattern: "_agent\\.velocity\\.magnitude"
---

<objective>
Verify the complete Phase 5 migration in Unity Play mode. Confirm the patrol animation restart bug is fixed and all existing snake behaviors still work correctly with the NavMesh system.

Purpose: Code changes in Plans 01-03 cannot be verified without runtime testing. This checkpoint confirms the root bug is resolved and there are no regressions before the branch is ready to merge.

Output: Human confirmation that v1.8.3 is working correctly in Play mode.
</objective>

<execution_context>
@C:/Users/Teilnehmer/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Teilnehmer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-movement-migration/05-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Play Mode Verification: Patrol bug fixed + all behaviors work</name>
  <action>
USER ACTION REQUIRED. Open Unity, enter Play mode in the GameLevel scene, and run through the following tests.

What was built in Plans 01-03 (SnakeAI.cs v1.8.3):
- Awake(): NavMeshAgent active (updatePosition=true, nextPosition synced to prevent teleport)
- SetState(): agent.isStopped/ResetPath per state (Dazed/Dead/AttackingEnemy stop+clear, Frozen stop+preserve, Idle/Aggressive/MovedAway resume)
- HasAgentArrived(): 4-condition arrival check (fixes remainingDistance=Infinity Unity bug)
- UpdatePatrol(): SetDestination + HasAgentArrived + SamplePosition validation
- FollowPlayer(): SetDestination at _chaseSpeed
- StartMoveAwayMovement(): SetDestination to _moveAwayTarget
- UpdateMovementAnimation(): velocity.magnitude check (not _isPatrolling bool — THIS is the bug fix)
- MoveTowardsSafe() deleted, _lastMoveDirection deleted
  </action>
  <verify>
Run these 5 tests in Play mode. ALL must pass:

TEST 1 — Core Bug Fix (most important):
1. Start Play mode. Watch any snake in GameLevel scene.
2. Observe: Snake begins patrolling (slither animation plays).
3. Let the snake approach a wall or obstacle.
4. EXPECTED: Snake navigates AROUND the obstacle. Animation continues smoothly.
5. PASS if: Animation does NOT restart from frame 0 when snake is near a wall.
6. FAIL if: Animation snaps back to start frame every time snake touches geometry.

TEST 2 — Console Clean:
1. Check Console window while in Play mode.
2. EXPECTED: Zero red errors. Zero yellow warnings (minor path warnings OK).
3. FAIL if: NullReferenceException, MissingFieldException, or NavMesh errors appear.

TEST 3 — Tune 1 (Move Away):
1. Walk player close to a snake (within ~8 units, line of sight).
2. Press and HOLD key 1. Release in the trigger zone.
3. EXPECTED: Snake slithers to its MoveAwayTarget position via NavMesh path.
4. PASS if: Snake moves smoothly to target (may navigate around obstacles).

TEST 4 — Tune 2 (Daze):
1. Walk player close to a different snake.
2. Press and HOLD key 2. Release in the trigger zone.
3. EXPECTED: Snake collapses (Die animation), stops moving completely, 8s timer.
4. PASS if: Snake stops moving immediately on Daze state entry.

TEST 5 — Failed Tune (Aggressive):
1. Press and HOLD key 1. Release AFTER the trigger zone (too late).
2. EXPECTED: Snake becomes aggressive (red color), chases player via NavMesh.
3. PASS if: Snake navigates toward player using pathfinding.

BONUS TEST 6 — Directional Animation (optional):
1. Watch a patrolling snake from above.
2. EXPECTED: Slither Forward when moving forward, Slither Left/Right when turning.
3. NOTE: If only Forward plays, that is acceptable — directional variants need animator transitions configured in Unity Editor separately.
  </verify>
  <done>
All 5 required tests pass: patrol animation does not restart when snake is blocked, console is clean, Tune 1 moves snake via NavMesh, Tune 2 stops movement on Daze, failed tune triggers NavMesh chase.
  </done>
  <resume-signal>Type "approved" if all 5 required tests pass (Tests 1-5). Describe any failures with test number if issues found. Test 1 (core bug fix) is the primary success criterion.</resume-signal>
</task>

</tasks>

<verification>
Phase 5 complete when:
- [ ] Patrol animation restart bug is FIXED (Test 1 passes)
- [ ] No console errors (Test 2 passes)
- [ ] Tune 1 Move Away works via NavMesh (Test 3 passes)
- [ ] Tune 2 Daze stops movement (Test 4 passes)
- [ ] Aggressive chase works via NavMesh (Test 5 passes)
</verification>

<success_criteria>
Human confirms in Play mode:
- Patrol animation does NOT restart when snake approaches a wall
- All Tune behaviors (1, 2, failed) still functional
- Console clean (no red errors)
</success_criteria>

<output>
After approval, create `.planning/phases/05-movement-migration/05-04-SUMMARY.md`

Then update STATE.md:
- Phase 5 complete
- Next: Phase 6 (Cleanup and Polish) — remove any remaining obsolete code
- Branch feature/enemy-setup ready for Phase 6
</output>
