---
phase: 12-code-review-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/_Project/Scripts/UI/TuneSliderUI.cs
  - Assets/_Project/Scripts/TuneSystem/TuneController.cs
autonomous: true

must_haves:
  truths:
    - "TuneSliderUI shows 'Shield' (not 'Attack') as the label for Tune 3 when the slider is displayed"
    - "TuneSliderUI has no reference to 'Freeze' or Tune 4 in the tune name switch"
    - "TuneController._unlockAllOnStart default value is false — the scroll unlock system is NOT bypassed in production"
    - "Both changes compile without errors in Unity 6 C#"
    - "No other logic in either file is changed — only the two targeted lines"
  artifacts:
    - path: "Assets/_Project/Scripts/UI/TuneSliderUI.cs"
      provides: "Corrected tune name labels in ShowSlider() method"
      contains: "Shield"
    - path: "Assets/_Project/Scripts/TuneSystem/TuneController.cs"
      provides: "Debug flag disabled by default"
      contains: "_unlockAllOnStart = false"
  key_links:
    - from: "TuneSliderUI.cs"
      to: "TuneConfig.cs SnakeEffect enum"
      via: "tune name labels must match Move/Daze/Shield"
      pattern: "Shield"
    - from: "TuneController.cs"
      to: "SpellUnlockSystem.cs"
      via: "_unlockAllOnStart = false means tunes are locked until scroll collected"
      pattern: "_unlockAllOnStart"
---

<objective>
Fix two critical production bugs identified in the expert code review:

**K1 — TuneSliderUI wrong tune names**: The ShowSlider() switch maps Tune 3 → "Attack" (wrong, was renamed to "Shield" in Phase 7) and references Tune 4 → "Freeze" (removed). Players see the wrong label when casting.

**K2 — TuneController debug flag active**: `_unlockAllOnStart = true` bypasses the entire scroll collection system. All 3 tunes are immediately available at game start, making scrolls pointless. Must be `false` in production.

Purpose: Eliminate correctness bugs before academic submission. Both fixes are 1-line changes with zero structural risk.
Output: TuneSliderUI.cs with correct "Shield" label, TuneController.cs with debug flag off by default.
</objective>

<execution_context>
@C:/Users/Teilnehmer/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Teilnehmer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Assets/_Project/Scripts/UI/TuneSliderUI.cs
@Assets/_Project/Scripts/TuneSystem/TuneController.cs
@Assets/_Project/Scripts/TuneSystem/TuneConfig.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix tune name labels in TuneSliderUI.cs</name>
  <files>
    Assets/_Project/Scripts/UI/TuneSliderUI.cs
  </files>
  <action>
In `TuneSliderUI.cs`, find the `ShowSlider()` method (or the equivalent method that sets the tune name label when the slider appears). Inside it is a switch expression that maps tuneNumber to a string tune name:

```csharp
string tuneName = tuneNumber switch
{
    1 => "Move",
    2 => "Daze",
    3 => "Attack",   // WRONG — Tune 3 is Shield since Phase 7
    4 => "Freeze",   // WRONG — Tune 4 was removed
    _ => $"Tune {tuneNumber}"
};
```

Make EXACTLY these two changes:
1. Change `3 => "Attack"` to `3 => "Shield"`
2. Remove the entire `4 => "Freeze",` line

Result must look like:
```csharp
string tuneName = tuneNumber switch
{
    1 => "Move",
    2 => "Daze",
    3 => "Shield",
    _ => $"Tune {tuneNumber}"
};
```

Update the version comment at the top of the file from v2.1 to v2.2 and add a version history entry:
`* - v2.2: Fix K1 — Tune 3 label corrected to "Shield", removed obsolete Tune 4 "Freeze" case`

Do NOT change anything else in the file.
  </action>
  <verify>
1. Open TuneSliderUI.cs — confirm the switch has exactly 3 named cases (Move, Daze, Shield) plus the default
2. Confirm "Attack" and "Freeze" no longer appear as switch case values
3. Confirm version comment now says v2.2
4. No other methods or properties are changed
  </verify>
  <done>TuneSliderUI.cs shows "Shield" for Tune 3. "Freeze" case removed. Version is v2.2.</done>
</task>

<task type="auto">
  <name>Task 2: Disable debug flag in TuneController.cs</name>
  <files>
    Assets/_Project/Scripts/TuneSystem/TuneController.cs
  </files>
  <action>
In `TuneController.cs`, find the serialized field:

```csharp
[SerializeField] private bool _unlockAllOnStart = true;
```

Change ONLY the default value from `true` to `false`:

```csharp
[SerializeField] private bool _unlockAllOnStart = false;
```

This field controls whether all tunes are bypassed at startup. The value `true` was a debug convenience and must not ship in the final build.

Also update or add a tooltip attribute if one exists to clarify it is a debug flag:
```csharp
[Tooltip("DEBUG ONLY — set false in production. If true, bypasses scroll unlock system and unlocks all tunes at start.")]
[SerializeField] private bool _unlockAllOnStart = false;
```

Update the version comment at the top of the file from v3.2 to v3.3 and add:
`* - v3.3: Fix K2 — _unlockAllOnStart default set to false (was debug=true, shipped by accident)`

Do NOT change anything else in the file.
  </action>
  <verify>
1. Open TuneController.cs — confirm `_unlockAllOnStart = false`
2. Confirm tooltip is present warning it is a debug flag
3. Confirm version is v3.3
4. No other fields, methods, or logic changed
  </verify>
  <done>TuneController._unlockAllOnStart defaults to false. Debug tooltip added. Version is v3.3.</done>
</task>

</tasks>

<verification>
1. TuneSliderUI.cs tune name switch: 1→"Move", 2→"Daze", 3→"Shield", _→$"Tune {tuneNumber}" — no "Attack", no "Freeze"
2. TuneController.cs `_unlockAllOnStart = false` — confirmed in serialized field declaration
3. Both files compile (no syntax errors introduced)
4. Version headers updated in both files
5. No other logic in either file is changed
</verification>

<success_criteria>
- Tune 3 is labeled "Shield" in-game when slider is shown
- All 3 tunes are locked at game start; scrolls must be collected to unlock them
- Zero compilation errors
- Only 2 files modified, minimal line count change
</success_criteria>

<output>
After completion, create `.planning/phases/12-code-review-fixes/12-01-SUMMARY.md`
</output>
